spring:
  application:
    name: testConsulApp
  cloud:
    consul:
      host: 127.0.0.1
      port: 8500


spring:
   profiles: docker
  application:
    name: testConsulApp
  cloud:
    consul:
      host: dev-consul
      port: 8500